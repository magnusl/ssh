#ifndef _SFTP_IMPL_H_
#define _SFTP_IMPL_H_

#include "sftp_impl.h"

namespace sftp
{
    /* Class:           sftp_impl_v3
     * Description:     SFTP version 3 implementation.
     */
    class sftp_impl_v3 :  public sftp_impl 
    {
    public:
        /*sftp_dir_request * dir(sftp_handle *, sftp_directory *);
        sftp_open_dir * open_dir(const wchar_t *, sftp_handle *);
        sftp_open_file * open_file(const wchar_t * path, sftp_handle * handle,  
            uint32 modes, const sftp_file_attributes & );
        sftp_close_request * close(sftp_handle * handle, bool);
        sftp_read_data * read(sftp_handle *, unsigned char *, uint32);
        */
    };
};


#endif