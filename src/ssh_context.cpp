#include "ssh_context.h"

namespace ssh
{

    /* Function:            ssh_context::load   
    * Description:          Loads the information from a file
    */
    bool ssh_context::load(const char * filename)
    {   
        return true;
    }
};